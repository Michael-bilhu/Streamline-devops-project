services:
  web:
    build: .
    ports:
      - "80:80"
    depends_on:
      - backend

  backend:
    build: ./backend
    ports:
      - "5000:5000"

  redis:
    image: redis
    volumes:
      - redis-data:/data

volumes:
  redis-data:
